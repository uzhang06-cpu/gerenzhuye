# 复制成 .env（已被 gitignore），本地开发用。
# 生产环境（Sealos）在应用的「环境变量」里配同样的键，不要提交这个文件。

# DeepSeek API key。不填的话 /api/llm/* 会返回 503，前端自动降级为手动选技能。
DEEPSEEK_API_KEY=

# 现行模型 id：deepseek-flash（便宜、支持 JSON 输出与 vision）或 deepseek-v4-pro。
# 留空则用 deepseek-flash。服务端启动时会打印账号下真实可用的模型，对不上会告警。
DEEPSEEK_MODEL=deepseek-flash

# 设为 1 时不调用真实模型，走本地模拟：
# 按别名对文本做匹配，产出的 id 天然合法，用来在没 key 的情况下跑通整条链路。
MOCK_LLM=0
